<?php
/**
 * PHPStan bootstrap file for AutoClose.
 *
 * @package WebberZone\AutoClose
 */

if ( ! defined( 'ACC_PLUGIN_VERSION' ) ) {
	define( 'ACC_PLUGIN_VERSION', '0.0.0' );
}

if ( ! defined( 'ACC_PLUGIN_FILE' ) ) {
	define( 'ACC_PLUGIN_FILE', '' );
}

if ( ! defined( 'ACC_PLUGIN_DIR' ) ) {
	define( 'ACC_PLUGIN_DIR', '' );
}

if ( ! defined( 'ACC_PLUGIN_URL' ) ) {
	define( 'ACC_PLUGIN_URL', '' );
}
